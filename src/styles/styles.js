import { StyleSheet } from "react-native";

export const globalStyles = StyleSheet.create({
    main: {
        flex: 1,
        paddingTop: 25,
    },
    title: {
        fontSize: 20,
        textAlign: 'center',
    }
})